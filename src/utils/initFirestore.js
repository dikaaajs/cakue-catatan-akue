// import { collection, addDoc } from "firebase/firestore";
// import { db } from "../config/fbConfig";

// export const initStore = (id) => {
//   const collRef = collection(db, catatan);
//   try {
//     const docRef = addDoc(collRef, {});
//   } catch (e) {}
// };
